import * as vscode from "vscode";
interface IConfig {
    locale?: string;
}
export declare class Localize {
    private options;
    private bundle;
    private extensionContext;
    constructor(options?: IConfig);
    /**
     * translate the key
     * @param key
     * @param args
     */
    localize(key: string, ...args: string[]): string;
    init(context: vscode.ExtensionContext): void;
    private format;
    private resolveLanguagePack;
}
export declare function init(context: vscode.ExtensionContext): void;
export declare function localize(key: string, ...args: string[]): string;
export {};
